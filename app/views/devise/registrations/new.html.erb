<% provide(:navbar_text, 'white') %>
<% provide(:home, 'img') %>
<div class="container-login mt-5">
  <h1 class="text-white text-center mb-5"><%= t('.sign_up') %></h1>
  
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= bootstrap_devise_error_messages! %>
  
    <div class="form-group w-75 mx-auto text-white">
      <%= f.email_field :email, autofocus: true, placeholder: "Email",
                                                 autocomplete: 'email', 
                                                 class: 'form-control form-text',
                                                 required: true,
                                                 maxlength: '30' %>
    
    <div class="form-group w-75 mx-auto">
      <%= f.fields_for :profile do |user| %>
        <%= user.text_field :name, autofocus: true, placeholder: "Name",
                                                   autocomplete: 'Name', 
                                                   class: 'form-control form-text',
                                                   required: true,
                                                   maxlength: '30' %>
      <% end %>
    </div>
      
    <div class="form-group w-75 mx-auto text-white">
      <%= f.email_field :email, placeholder: "Email", autocomplete: 'email', 
                                                      class: 'form-control form-text',
                                                      required: true,
                                                      maxlength: '30' %>
    </div>
  
    <div class="form-group w-75 mx-auto text-white">
      <%= f.password_field :password, autocomplete: 'current-password', 
                                             placeholder: "Password",
                                             class: 'form-control form-text',
                                             required: true,
                                             minlength: @minimum_password_length,
                                             maxlength: '30' %>
  
      <% if @minimum_password_length %>
        <small ><%= t('devise.shared.minimum_password_length', count: @minimum_password_length) %></small>
      <% end %>
    </div>
  
  
    <div class="form-group">
      <%= f.label :password_confirmation %>
      <%= f.password_field :password_confirmation, autocomplete: 'current-password',
                                                   placeholder: "Password onfirmation",
                                                   class: 'form-control',
    <div class="form-group w-75 mx-auto text-white">
      <%= f.password_field :password_confirmation, autocomplete: 'current-password', 
                                                   placeholder: "Password Confirmation",
                                                   class: 'form-control form-text',
                                                   required: true,
                                                   minlength: @minimum_password_length,
                                                   maxlength: '30' %>
    </div>

    <div class="form-group w-75 mx-auto text-white">
      <%= f.submit t('.sign_up'), class: 'btn btn-primary btn-block button' %>
    </div>
  <% end %>
  
  <%= render 'devise/shared/links' %>
</div>